# PWA 快速記事驗證紀錄

## 已驗證

- `/quick-note` 已於桌面瀏覽器載入，呈現本機草稿、複製與清除操作。
- 在快速記事輸入測試文字後重新整理，文字會由瀏覽器的 `localStorage` 還原。
- 375px 行動版完整頁截圖顯示標題、草稿區、私隱說明與前往完整編輯器連結皆維持可讀佈局。
- 生產建置會產生 `manifest.webmanifest`、`registerSW.js` 與 `sw.js`。

## 尚待驗證

- 在實際斷網與重新連網的瀏覽器條件下，驗證連線狀態文字由「連線中」切換至「離線草稿模式」再切回。
