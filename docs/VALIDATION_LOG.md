# 驗證紀錄

## 2026-08-19：未登入編輯器

| 檢查項目 | 結果 | 方法 |
| --- | --- | --- |
| `auth.me` 未登入回應 | 通過 | 對主開發預覽發送 tRPC 請求，回傳 `200` 與 `null`，耗時約 0.07 秒。 |
| `/editor` 未登入畫面 | 通過 | 瀏覽器直接開啟 `/editor`，顯示「開啟你的成長檔案」登入卡與「登入並開始編輯」按鈕。 |

首次視覺截圖可能捕捉到 React 查詢開始前的短暫載入殼層；不應將該初始畫面誤判為永久載入。已登入日記資料與刻意觸發查詢失敗後的重試流程仍需在可用的登入工作階段中驗證。

## 2026-08-19：主預覽與實際瀏覽器工作階段差異

主預覽的自動登入殼層會顯示注入的帳號側欄，但重啟後不一定帶有可供 API 使用的工作階段 cookie，因此日記區可能停留在初始載入畫面。以隔離瀏覽器工作階段直接開啟 `/editor` 時，頁面正確顯示未登入卡與「登入並開始編輯」按鈕；此結果確認未登入分支可用，不能將主預覽殼層的瞬時畫面視為已登入日記載入失敗。

## 2026-08-19：375px 手機版入口與本機草稿

| 檢查項目 | 結果 | 方法 |
| --- | --- | --- |
| `/quick-note` 窄螢幕入口與可讀性 | 通過 | 以 375×812 截圖確認標題、離線說明、記事輸入區、已自動儲存提示、清除與複製操作，以及「前往完整成長日記編輯器」均未溢出或相互遮擋。 |
| 快速記事重新整理還原邏輯 | 通過（自動化） | `createQuickNoteDraft` 序列化為 localStorage 字串後，可由 `parseQuickNoteDraft` 完整還原原始內容與更新時間；對應 Vitest 回歸測試通過。 |
| `/editor` 375px 完整日記工作區 | 受工作階段限制 | 截圖捕捉到初始載入殼層；該環境未帶有有效 API 工作階段 cookie，故不能據此判定已登入日記的分頁、事件選取或新事件入口。 |

載入逾時與查詢錯誤已抽成 `getDiaryLoadStatus` 純函式，並確認錯誤與逾時優先於載入殼層，以保證畫面進入可提供重試／重新載入操作的恢復分支；實際 UI 互動仍須於有效登入工作階段進行最後確認。

## 2026-08-19：公開故事資料抵達後的結構性回歸

| 檢查項目 | 結果 | 方法 |
| --- | --- | --- |
| 公開封面 | 通過（自動化） | `SharedStory` 在設定封面 URL 與標題時，輸出封面圖片及具意義的替代文字。 |
| 三種閱讀版型 | 通過（自動化） | 對 `editorial`、`gallery` 與 `minimal` 三種設定，以模擬成功分享資料靜態渲染，確認個別 `shared-layout-*` 類別與封面標題均會輸出。 |
| 真實公開連結的 375px 視覺畫面 | 待驗證 | 目前工作階段沒有可用的公開故事 slug 與已登入資料；不建立虛構故事或硬編碼假資料，待有真實公開連結後再進行視覺確認。 |

## 2026-08-19：家庭邀請頁樣式模組歸位

| 檢查項目 | 結果 | 方法 |
| --- | --- | --- |
| `/family-invite` 375px 無工作階段入口 | 確認限制 | 截圖顯示既有的「正在翻開你的成長檔案」載入殼層；由於頁面使用受保護工作區框架且目前無有效工作階段，無法在此環境檢視一次性邀請按鈕的實際內容。樣式已由全域檔移入家庭協作模組，並經型別檢查與正式建置確認可載入。 |

## 2026-08-19：事件工作室樣式重構後的 375px 入口

| 檢查項目 | 結果 | 方法 |
| --- | --- | --- |
| `/quick-note` 375px 重新檢查 | 通過 | 截圖確認標題、連線提示、本機草稿輸入區、儲存狀態、清除／複製按鈕與隱私說明皆保持可讀，未見橫向溢位。 |
| `/editor` 375px 未登入／無工作階段入口 | 確認限制 | 截圖停留在「正在翻開你的成長檔案」初始殼層；此工作階段沒有有效 API cookie，不能據此推論手機分頁、事件選取或新事件入口的已登入行為。 |

## 2026-08-19：主預覽工作階段盤點

主預覽近期的網路請求紀錄持續顯示 `auth.me` 回傳 `200` 與 `null`，且未發現可用的 `diary.get` 已登入請求。故目前無法在此瀏覽器工作階段實證已登入日記內容、逾時重試或查詢失敗恢復；這些項目保留為需要有效登入 cookie 的待辦，不以未登入載入殼層推論功能失敗。

已嘗試啟用使用者既有瀏覽器工作階段以完成此回歸範圍，但連接器啟用建議未獲確認，因此沒有對連接器或帳號狀態做任何變更。仍維持目前的自動化與未登入／本機草稿驗證證據。

使用者已於 2026-08-19 指示跳過目前受有效登入工作階段限制的瀏覽器互動驗證。因此，已登入 `diary.get`、逾時／查詢失敗重試、375px 編輯器分頁與快速記事輸入後重新整理還原會保留為延後待辦，而非標記為完成或失敗。

## 2026-08-19：重構後公開首頁視覺基準

| 檢查項目 | 結果 | 方法 |
| --- | --- | --- |
| 桌面首頁 | 通過 | 1280×720 截圖確認導覽、主標、行動按鈕與時間軸示意版面正常呈現。 |
| 375px 首頁 | 通過 | 截圖確認行動導覽按鈕、主標、說明、兩個行動按鈕與時間軸示意在窄視窗下仍可讀，未見可見橫向溢位。 |

## 2026-08-19：平行多軌時間軸與洞察

| 檢查項目 | 結果 | 方法 |
| --- | --- | --- |
| 多軌資料衍生 | 通過（自動化） | `multitrackTimeline` 單元測試覆蓋四軌分組、技能反向篩選、未來時空膠囊鎖定與里程碑統計。 |
| 既有日記相容性 | 通過（自動化） | 完整 `pnpm check`、33 個測試檔共 96 項 Vitest 與正式建置通過；路由測試確認舊事件會獲得安全的新欄位預設值。 |
| 已登入多軌編輯器視覺互動 | 延後 | 主預覽仍沒有有效 `diary.get` 工作階段。首頁桌面截圖正常；`/editor` 捕捉到既有初始載入殼層，不能據此判定多軌節點、階段切換或技能篩選的已登入視覺結果。 |

## 2026-08-19：三視角與分級位置隱私

| 檢查項目 | 結果 | 方法 |
| --- | --- | --- |
| 時間軸／Bento／足跡資料衍生 | 通過（自動化） | `timelineViews` 單元測試確認里程碑權重可衍生 Bento 視覺層級、地點文字可於私有範圍分組，且手動填入的 E6 經緯度會投影至確定性的世界格網位置。 |
| 位置輸入與公開降級 | 通過（自動化） | 表單工具測試覆蓋座標範圍驗證；分享資料層測試確認精確座標一律從公開回應移除，只有 `city` 精度可保留地點文字。 |
| 生產可建置性 | 通過 | 完整 `pnpm check`、34 個測試檔共 101 項 Vitest 與正式建置通過。 |
| 已登入三視角視覺切換 | 延後 | 目前瀏覽器未帶有效日記工作階段，故未以初始載入殼層推論三視角切換、Bento 卡片或私有座標節點的實際登入互動結果。 |

## 2026-08-19：Before–After 與章節 BGM

| 檢查項目 | 結果 | 方法 |
| --- | --- | --- |
| Before–After 資料選擇 | 通過（自動化） | `beforeAfter` 單元測試確認僅在同一對比群組具有至少兩張影像時，才選取最早與最新事件，避免呈現誤導性對比。 |
| 章節 BGM 版本資料 | 通過（自動化） | 事件版本快照測試確認 BGM 標題與直接音訊網址可保存、解析與還原；舊快照缺少欄位時仍安全視為空值。 |
| 自動播放保護 | 通過（程式檢查） | 預覽採原生 `<audio controls preload="metadata">`，沒有 `autoplay` 屬性、程式化 `.play()` 呼叫或滾動觸發音訊；使用者必須自行點擊播放。 |
| 已登入滑桿與音訊控制視覺互動 | 延後 | 目前工作階段沒有有效日記資料，故尚未在真實事件媒體與音訊網址下進行瀏覽器端拖曳及手動播放確認。 |

## 2026-08-19：分級分享與社群卡後的公開入口檢查

| 檢查項目 | 結果 | 方法 |
| --- | --- | --- |
| 375px 公開首頁 | 通過 | 以 375×812 全頁截圖確認行動導覽、主標、時間軸示意、故事卡及頁尾仍可讀，未見可見的水平溢位。 |
| 分級分享與社群卡資料規則 | 通過（自動化） | 分享資料層與社群卡工具測試覆蓋 public／link 範圍選取、未解鎖內容遮罩，以及年度卡僅使用公開事件統計與公開回顧摘要。 |
| 已登入卡片下載與範圍選擇器互動 | 延後 | 目前瀏覽器沒有有效 `diary.get` 工作階段；不以初始載入殼層推論編輯器中的選擇器、SVG 下載按鈕或年度卡控制項行為。 |

## 2026-08-19：延後驗證工作階段再次確認

直接開啟 `/editor` 後，頁面仍顯示「開啟你的成長檔案」及「登入並開始編輯」未登入入口；沒有可用的日記工作階段可以驗證已登入載入、錯誤恢復、手機工作區、分級分享選擇器或社群卡下載互動。此結果與使用者先前選擇跳過工作階段受限驗證的邊界一致，未變更帳號或連接器設定。

## 2026-08-19：375px 快速記事重新整理還原

在 375px 行動視窗的 `/quick-note` 輸入「375px 重新整理還原驗證：這段草稿只留在本機。」後重新開啟相同頁面；文字、字數與「已自動儲存」提示均被完整還原。此驗證不需要登入，且再次確認草稿只保存在當前瀏覽器，不會上傳至日記或分享頁。

## 2026-08-19：公開故事封面與 375px 閱讀版型

使用隔離 `AUTH_DRIVER=local` 暫時帳號建立一筆公開事件，透過正式分享設定與封面上傳程序保存自訂封面標題、封面圖和 `gallery` 版型；主開發站的公開閱讀 API 回傳封面 URL、標題、版型和事件，375px 全頁截圖確認封面、標題、階段卡、事件卡和頁尾均可閱讀，未見可見水平溢位。其後切換為 `editorial` 版型並再次取得 375px 全頁截圖，結果同樣可讀。`minimal` 版型此前已在 375px 驗證；因此三種閱讀版型的行動回歸均已完成。

驗證完成後，已透過帳號刪除程序清除暫時帳號、日記、事件、分享和關聯資料；原公開故事網址回傳「這段故事目前無法閱覽」，確認沒有遺留公開內容。

## 2026-08-19：隔離 authenticated `/editor` 的 375px 回歸

新增 `test:e2e:isolated` 指令，要求以 HTTPS 的 `AUTH_DRIVER=local` 測試網址提供 `CHRONICLE_E2E_BASE_URL`。該瀏覽器回歸會建立臨時 local-auth 帳號與單一私有事件，將視窗設為 375×812，並實測索引分頁、事件選取後的預覽、從索引新增事件切換至撰寫分頁，以及日記內容實際載入。

同一腳本以網路攔截先延遲 `diary.get` 超過 10 秒，確認逾時恢復介面與「重新嘗試」可回到已登入工作區；再中止 `diary.get`，確認查詢錯誤畫面與重試同樣可回復，並在第二次錯誤實際點擊「重新載入頁面」後確認整頁重新進入已登入工作區。指令於本輪執行通過，所有十個檢查均成功；結束時呼叫帳號刪除程序，暫時帳號與其日記、事件及關聯資料均已清除。主開發站／正式 OAuth 工作階段的 `diary.get` 成功載入仍另列為待辦，未以隔離測試取代。

## 2026-08-19：主開發站正式 OAuth 工作階段再次確認

在不觸發登入、不修改帳號資料的被動檢查下，主開發站 `/editor` 仍顯示「開啟你的成長檔案」與「登入並開始編輯」未登入入口，表示此瀏覽器沒有可供 API 使用的 OAuth session cookie。因此，無法在不違反使用者先前跳過登入受限驗證的決定下，完成主開發站／正式 OAuth 的 `diary.get` 成功載入實證；此待辦將繼續保留，隔離 local-auth 證據不會替代它。

## 2026-08-19：首頁登入按鈕修復

問題根因是首頁導覽列的「登入」按鈕仍保留模板時期的提示訊息，只顯示「登入功能可於後續串接會員系統」，沒有呼叫 OAuth 導向。已改為直接從使用者點擊事件呼叫既有 `startLogin`，維持一次性 nonce cookie、`state`、`appId`、`redirectUri` 和 `type=signIn` 的既有安全合約。

新增 `client/src/const.test.ts`，驗證登入導向會寫入短效 nonce cookie 並建立正確登入 URL；新增 `test:e2e:login`。主開發站的隔離 Chromium 回歸已實際點擊首頁「登入」，並確認導向 `https://manus.im/app-auth`，包含 `appId`、callback、`state` 與 `signIn` 參數。完整 `pnpm check`、41 個測試檔共 117 項 Vitest 和正式建置均通過。

## 2026-08-19：公開入口路由延後載入

將 `DiaryEditor`、`FamilyInvite`、`QuickNote`、`SharedStory` 與 `NotFound` 改為 route-level lazy loading，首頁保留為直接載入；載入中的非首頁路由會顯示可讀的「正在整理閱讀頁面…」邊界。正式建置確認這些路由各自輸出為獨立 chunk，編輯器與其匯出依賴不再進入首頁初始 JavaScript。

新增 `RouteLoadingState` 靜態回歸測試。完整 `pnpm check`、42 個測試檔共 118 項 Vitest 與正式建置通過；首頁已在 1280×720 與 375×812 截圖確認可讀，延後載入的 `/quick-note` 在 375×812 亦成功開啟且無可見水平溢位。此驗證未觸發正式 OAuth 登入。

## 2026-08-19：首頁非首屏媒體延後載入

首頁三張故事案例圖片現在使用原生 `loading="lazy"` 與 `decoding="async"`。這些圖片仍保留空替代文字，因為相鄰標題已提供相同敘事內容，不會向輔助技術重複朗讀。新增首頁靜態渲染回歸，確認三張案例圖片均維持延後載入與非同步解碼設定。

完整 `pnpm check`、43 個測試檔共 119 項 Vitest 與正式建置均通過；首頁全頁桌面與 375px 行動截圖皆確認案例區、文字與下方區塊保持可讀且版面穩定。此改善未觸發登入流程。

## 2026-08-19：首頁字型載入優化

將 DM Serif Display、IBM Plex Mono 與 Noto Sans TC 的 Google Fonts 宣告從 CSS `@import` 移至文件 `<head>`，並新增 `fonts.googleapis.com` 與 `fonts.gstatic.com` 的預連線；既有 `display=swap`、字型家族與權重均維持不變。這避免 CSS import 形成額外的字型樣式表載入相依，且在字型尚未到達時保留可讀 fallback。

新增 `fontLoading.test.ts` 確認預連線、三種設計字型、`display=swap` 與 CSS import 移除。完整 `pnpm check`、44 個測試檔共 120 項 Vitest 與正式建置通過；1280×720 與 375×812 首頁截圖確認展示字、內文與資料微文案均保持可讀。此驗證未觸發登入流程。

## 2026-08-19：行動導覽可及性

首頁行動選單按鈕現提供 `type="button"`、`aria-controls="primary-navigation"` 與即時 `aria-expanded` 狀態；展開選單時按 Escape 會收合，點擊任一導覽連結、登入或「開始建立」也會先收合選單。選單開啟時不會讓左右方向鍵繼續改變下方時間帶焦點。

首頁靜態回歸確認初始收合狀態、控制項關聯與 Escape 判定；新增 `test:e2e:mobile-nav` 以 375×812 Chromium 實際驗證展開、Escape 收合與「故事案例」連結收合。完整 `pnpm check`、44 個測試檔共 121 項 Vitest、正式建置與瀏覽器回歸均通過，且未觸發登入流程。

## 2026-08-19：首頁範例入口

首頁英雄區的「觀看範例」已由暫時通知改為語意正確的 `#stories` 錨點連結，讓使用者直接抵達三則故事案例，而不再誤以為已開啟不存在的範例時間軸。靜態首頁回歸確認連結目標與暫時文案移除；既有 375px Chromium 公開首頁回歸同步實際點擊入口，確認 URL 切換至 `#stories`，然後繼續驗證行動導覽行為。

完整 `pnpm check`、44 個測試檔共 122 項 Vitest、正式建置與瀏覽器回歸皆通過，且未觸發登入流程。

## 2026-08-19：首頁熱更新模組邊界

行動導覽的 Escape 判定已由 `Home.tsx` 移至 `client/src/lib/homeNavigation.ts`。如此首頁模組只保留 React 元件的預設匯出，避免 Vite 因同時偵測元件與非元件匯出而使 Fast Refresh 降級為整頁重新載入。首頁回歸測試改由純函式模組匯入判定邏輯。

重新啟動開發服務後，型別檢查、44 個測試檔共 122 項 Vitest、正式建置與公開首頁桌面截圖皆通過；首頁可正常渲染，未觸發登入流程。

## 2026-08-19：首頁時間帶方向鍵焦點保護

首頁的全域左右方向鍵現在只會在焦點不在連結、按鈕、輸入、選單、文字區或可編輯元素時切換時間帶事件。這保留了頁面空白區的鍵盤探索，同時避免使用者瀏覽導覽或操作按鈕時意外改變時間帶內容。

新增 `homeNavigation.test.ts` 覆蓋一般容器、連結、按鈕、可編輯元素與非方向鍵。375×812 Chromium 公開首頁回歸實際確認「觀看範例」連結取得焦點時按右方向鍵不改變目前事件；解除焦點後按右方向鍵會正常切換事件。完整 `pnpm check`、45 個測試檔共 124 項 Vitest、正式建置與瀏覽器回歸均通過，且未觸發登入流程。

## 2026-08-19：首頁頁內導覽與減少動態偏好

首頁頁內錨點維持原生平滑捲動；新增 `prefers-reduced-motion: reduce` 覆寫，當使用者要求減少動態效果時會改為立即定位。這讓「觀看範例」及主要導覽的同頁跳轉保有一致節奏，同時尊重系統層級的動態偏好。

新增 `indexStyles.test.ts` 驗證預設平滑捲動與減少動態覆寫並存。完整 `pnpm check`、46 個測試檔共 125 項 Vitest 與正式建置均通過；此改善不觸發登入流程。

## 2026-08-20：正式 OAuth 入口 CloudFront 403

使用者在公開預覽首頁點擊「登入」後，瀏覽器正確導向 `https://manus.im/app-auth`，並帶有專案 `appId`、目前預覽來源生成的 `redirectUri`、一次性 `state` 與 `type=signIn` 參數。頁面隨即回傳 CloudFront 的「403 ERROR — The request could not be satisfied」；再次直接開啟不含任何查詢參數的 `https://manus.im/app-auth`，結果同樣是 CloudFront 403。

因此，本次拒絕發生在外部 OAuth 入口抵達專案 callback 之前，而不是由 Chronicle 的 nonce／state callback 防偽檢查所產生；目前程式仍使用 `window.location.origin` 建立回呼網址，符合既有 OAuth 整合契約。正式 OAuth `diary.get` 實證繼續延後，待外部登入入口恢復可用後再進行。

## 2026-08-20：公開首頁、離線快速記事與社群預覽

| 檢查項目 | 結果 | 方法 |
| --- | --- | --- |
| 公開首頁鍵盤與行動導覽 | 通過 | `test:e2e:mobile-nav` 以 375×812 Chromium 驗證跳至主要內容、行動選單收合、故事案例、時間帶鍵盤探索、內部控制項焦點保護與分類篩選的 `aria-pressed` 更新。 |
| 減少動態偏好 | 通過 | 同一回歸啟用 `prefers-reduced-motion: reduce`，確認工作台過場縮短至近乎立即，故事圖片 hover 不再縮放。 |
| 離線快速記事入口與資料處理說明 | 通過 | 首頁提供 `/quick-note` 入口，且明確關聯「草稿只保存在目前裝置；準備好後可複製並整理成正式事件」說明；瀏覽器回歸確認入口、說明與路由可用。 |
| 社群分享中繼資料 | 通過（自動化） | Open Graph 與 Twitter 圖片均指向 Chronicle 品牌化時間帶視覺，並提供中文替代文字；`socialMetadata.test.ts` 已覆蓋。 |

完整 `pnpm check`、48 個測試檔共 133 項 Vitest、`pnpm build` 與上述公開首頁瀏覽器回歸均通過。這些檢查均未觸發正式 OAuth 登入；正式 `diary.get` 載入仍以本文件先前所述的外部 403 限制追蹤。
