# Chronicle 成長日記編輯器工作清單

- [x] 確認 GitHub 連線與可用的開源同步流程。
- [x] 升級專案為具備帳號、資料庫與檔案儲存的基礎架構。
- [x] 定義成長事件、標籤、媒體與個人時間軸的資料模型。
- [x] 建立成長日記資料表並產生、套用資料庫遷移。
- [x] 實作圖片上傳與儲存流程，保存檔案金鑰、網址與媒體資料。
- [x] 新增受保護的事件、標籤與媒體 tRPC 程序及資料庫查詢輔助。
- [x] 建立編輯器工作區：時間軸清單、事件表單、圖片與標籤管理。
- [x] 實作新增、更新、刪除、排序與篩選事件的互動。
- [x] 驗證桌面與手機版體驗、型別檢查及正式建置。
- [x] 建立最後檢查點並同步至 GitHub 開源儲存庫。
- [x] 設計並持久化人生階段、分享模式、分享代碼與公開事件設定。
- [x] 建立依事件時間自動分群的人生階段總覽。
- [x] 建立分享設定介面，支援私密、公開與持有私密連結者可閱覽模式。
- [x] 建立公開閱讀頁，並驗證分享連結僅顯示允許公開的事件。
- [x] 實作完整成長史 PDF 與長圖片匯出。
- [x] 擴充匯出內容，包含事件影像、標籤、地點與完整敘事。
- [x] 實際驗證 PDF 與長圖片下載流程及輸出內容。
- [x] 建立暫時分享案例，端對端驗證 public、缺少或錯誤 token、正確 token 與事件可見度。
- [x] 建立可閱讀分享案例，檢視分享閱讀頁在桌面與手機版的事件、標籤、圖片與版面。
- [x] 同步最終驗證結果至 GitHub 開源儲存庫。
- [x] 讀取內建 AI 整合規範，設計僅使用指定人生階段事件的回顧生成流程。
- [x] 持久化人生階段邊界、AI 回顧內容、分享密碼雜湊、到期時間與存取計數。
- [x] 建立 AI 成長回顧介面，支援按階段生成、重新生成與手動保留文字。
- [x] 實作拖曳式階段邊界及事件排序，並將調整結果儲存至時間軸。
- [x] 建立進階分享設定，支援密碼保護、到期日、連結重設與存取紀錄檢視。
- [x] 驗證 AI 生成、排序互動、分享安全與桌面／手機閱讀頁，並同步 GitHub。
- [x] 排除並驗證編輯器在新增 AI 與分享資料表後的日記載入流程。
- [x] 建立 AI 回顧可手動編輯與保存的機制，確認重新整理後仍保留使用者調整。
- [x] 在受保護使用者資料範圍驗證日記載入、AI 回顧、階段邊界與事件拖曳排序的持久化。
- [x] 在已登入的 /editor 實際確認日記內容載入，且不再停留於登入或載入畫面。
- [x] 在瀏覽器實測 AI 手動編輯、階段邊界拖曳與事件拖曳排序，並於重新整理後確認結果保留。
- [x] 在可檢視的 375px 手機版登入 /editor 中確認日記、人生階段與事件工作區正常載入且版面未溢位。
- [x] 完成跨裝置實測後儲存檢查點並同步本輪功能至 GitHub。
- [x] 限制拖曳式人生階段邊界為有效時間範圍，避免結束年份早於開始年份。
- [x] 建立日記事件的全文搜尋與開始／結束日期區間篩選。
- [x] 支援事件多張圖片的拖曳排序與每張圖片說明文字編輯。
- [x] 建立年度回顧模板，彙整指定年份的事件與成長摘要。
- [x] 支援公開故事的封面圖片、封面標題與版面風格自訂。
- [x] 在瀏覽器桌面版實測全文搜尋、日期區間篩選、多圖說明編輯與拖曳排序，並在重新載入後重新選取媒體事件確認順序與說明保留。
- [ ] 在瀏覽器桌面與 375px 手機版實測公開故事封面、封面標題及三種閱讀版型（隔離 local-auth 已確認桌面 editorial／gallery／minimal 與自訂標題；主預覽 375px 已確認 minimal 版型資料讀取後可讀且無可見溢位，封面圖片與其餘兩種版型的行動視窗仍待驗證）。
- [x] 優先儲存目前版本並同步本輪功能至 GitHub；後續再補足受保護編輯器的帳號層級互動驗證。
- [x] 將 Chronicle 儲存庫同步至使用者本機 `G:\AICODE\chronicle-growth-diary`，並在本機執行安裝、型別檢查、測試與建置。
- [x] 建立 `AGENTS.md` 與安全的本機設定指南，明確記錄架構、設計規範與環境變數；受管理環境的秘密不提交 `.env.example`。
- [x] 建立 GitHub Actions CI、貢獻指南、Issue／PR 模板與開源倉庫中繼資料。
- [x] 移除已確認未使用的 AIChatBox、Map、ComponentShowcase、圖表／輪播元件及相關依賴。
- [x] 全面盤點 `client/src/components/ui`、模板頁與 `package.json` 依賴的實際引用，移除其餘未使用的模板元件與套件，並記錄清理清單。
- [x] 在完成全面清理後重新執行 `pnpm check`、`pnpm test`、`pnpm build`，確認正式版本與本機副本皆通過。
- [x] 抽象儲存、認證與 LLM 提供者，維持現有 Manus 路徑相容並提供本機替代實作。
- [x] 建立可選擇的儲存提供者介面，保留 Forge 預設並支援 S3／MinIO 相容端點。
- [x] 建立可選擇的 LLM 提供者設定，保留 Forge 預設並支援 OpenAI-compatible API。
- [x] 建立認證提供者介面，將 tRPC context 與 OAuth callback 從具體 SDK 解耦並維持既有 session 合約。
- [x] 實作本機 email／密碼註冊與登入，使用安全雜湊、唯一 email 約束與既有簽章 session cookie。
- [x] 建立 MySQL、物件儲存與應用程式的可自架組態，並提供備份與還原文件。
- [ ] 拆分日記編輯器、樣式與後端資料存取層，補足路由、整合與端對端測試安全網。
- [x] 將集中於 `server/routers.ts` 的日記與分享程序拆分為專責路由模組，維持既有 tRPC API 型別與驗證規則。
- [x] 從 `DiaryEditor.tsx` 抽離日期／表單／圖片純函式與獨立畫面元件，降低主畫面狀態複雜度。
- [x] 為日記路由輸入驗證、篩選／排序與載入失敗恢復補上回歸測試：涵蓋批次匯入、事件／圖片排序上限、不支援圖片 MIME，以及快照讀取錯誤傳遞至前端恢復介面。
- [x] 抽離並測試全文搜尋、日期區間、標籤／類型篩選及 custom／newest／oldest 排序邏輯。
- [x] 為日記建立／更新、圖片排序、分享設定及載入失敗恢復新增整合流程測試：以受保護 tRPC caller 串接建立、更新、圖片上傳與排序、分享設定，並驗證快照讀取錯誤可交由前端恢復介面處理。
- [x] 實作 PWA、快速記事、離線草稿與行動版分頁式編輯體驗；窄視窗輸入後還原與分頁可及性驗證另行追蹤。
- [x] 在 `/editor` 建立手機專用的索引、撰寫、預覽分頁導覽，維持桌面三欄工作區不變。
- [ ] 驗證手機分頁切換、事件選取與新事件入口的可及性與響應式佈局。
- [x] 建立可安裝的 Web App Manifest 與 service worker 離線殼層。
- [x] 建立 `/quick-note` 本機快速記事頁面，支援 localStorage 草稿與手動複製內容。
- [x] 在行動與桌面預覽驗證快速記事及離線提示狀態（已直接檢視 375px 截圖：連線提示、標題、輸入區、字數與操作按鈕均可讀，無可見橫向溢位）。
- [x] 實際觸發 `/quick-note` 的 offline／online 狀態，驗證提示文字及切換行為。
- [ ] 分別在桌面與 375px 手機視窗確認快速記事輸入、重新整理還原與提示狀態（桌面已完成；375px 已確認空白草稿、提示與佈局，仍需窄視窗輸入後重整還原）。
- [x] 實作 JSON／Markdown／媒體匯出入、事件版本歷程與帳號級刪除：JSON／Markdown 保留受限內容，媒體採安全 ZIP manifest 封存，事件可還原版本，帳號刪除採明確確認與級聯清理。
- [x] 驗證 Chronicle JSON 匯入檔版本與事件欄位，預覽後批次建立事件，不匯入媒體位元組、分享資訊或帳號私密資料。
- [x] 定義版本化 JSON 與 Markdown 日記匯出格式，僅保留內容、標籤、媒體 URL 與回顧，不含分享憑證或帳號私密資料。
- [x] 在編輯器提供 JSON 與 Markdown 下載操作並測試格式、日期、文字跳脫與私隱欄位排除。
- [x] 實作可關閉、可刪除且隱私優先的 AI 年度回顧、寫作引導、標籤、照片說明與側寫能力：AI 可關閉／刪除，年度生成僅使用指定年份事件，寫作引導在本機套用，標籤／照片說明可編輯，側寫僅保存標題與副標題。
- [x] 提供日記擁有者可編輯的個人成長側寫（標題與副標題），明確不蒐集出生年月日、聯絡方式或其他敏感個資，且共同成員不可修改。
- [x] 實作並驗證真正的 AI 年度回顧流程，讓擁有者可依指定年份生成、保存與刪除回顧；年度模板與人生階段 AI 回顧不視為其替代品。
- [x] 新增日記層級的 AI 啟用偏好，並在生成前於伺服器端阻擋已關閉 AI 的請求。
- [x] 提供已保存階段回顧的刪除操作與清楚資料處理提示，不影響原始日記事件。
- [x] 建立噗浪與社群匯出檔的草稿匯入工作台、去重與重大事件候選流程：支援本機 JSON／CSV、來源 ID 去重、重大事件提示、預覽與人工確認後的私人事件建立。
- [x] 實作家庭共寫的成員權限、邀請、註解與稽核紀錄：一次性 email 綁定邀請、接受入口、角色調整／移除、事件註解與不可變稽核紀錄均已完成。
- [x] 完成雙語開源文件、SEO、社群維運流程與各階段 GitHub 同步：中英文 README 補入媒體封存與安全入口，新增私密弱點回報規範與 Dependabot 依賴更新設定，並在每輪完成後同步 GitHub。
- [x] 建立英文 README 與社群行為準則，提供清楚的功能、隱私、在地開發與貢獻入口。
- [x] 補足網站 description、canonical、Open Graph、Twitter Card 與 robots metadata，避免私人編輯器頁進入搜尋索引。
- [ ] 在主開發站或明確約定的正式工作階段確認 `diary.get` 會實際送出並成功載入日記內容；隔離 local-auth 驗證另行記錄。
- [ ] 實際觸發並驗證 `/editor` 的載入逾時恢復介面與重試／重新載入操作。
- [ ] 補做未登入登入卡、已登入日記內容與查詢失敗恢復狀態的瀏覽器端回歸檢查（主預覽未登入登入卡已於 2026-08-19 確認；已登入內容與查詢失敗恢復仍待驗證）。
- [x] 診斷並修復登入按鈕無法完成 OAuth 流程的問題，確認 callback、cookie 與 `/editor` 回復路徑。
- [x] 在使用者無法於虛擬機輸入帳密的限制下，以自動化 OAuth callback／session 測試與未登入介面檢查完成登入回歸驗證。
- [x] 修正事件標籤輸入欄按 Enter 時可能同時提交表單、造成重複建立事件的問題，並補上回歸測試。
- [x] 使用隔離 local-auth 帳號完成登入、日記載入、搜尋、日期篩選、雙圖說明、圖片拖曳排序與行動分頁邏輯回歸；測試帳號與級聯關聯資料已清除。
- [x] 修復 Quick Note 匯入遺失的連線狀態模組，並重新驗證開發載入、型別檢查、測試與正式建置。
- [x] 釐清公開故事封面圖片持久化流程：實際 `updateSharing`、`uploadCoverImage`、重新讀取與公開閱讀頁均已驗證成功；先前 SQL 查核與隔離 local-auth 伺服器使用不同資料連線而造成誤判。
- [x] 為公開故事封面上傳／儲存流程新增自動化回歸測試，覆蓋 `updateSharing`、`uploadCoverImage` 及重新讀取後 `publicCoverUrl` 存在。
- [x] 以隔離 local-auth 帳號完成公開故事封面、封面標題、desktop 三種版型與 375px minimal 版型驗證；測試帳號及其資料庫關聯資料已級聯清除。
- [x] 將隔離回歸與公開故事封面測試版本鏡像至 GitHub `main`（`724d718`），並於 Windows 本機副本完成 `check`、50 項測試與正式建置。
- [x] 完成帳號級刪除確認與事件版本歷程：介面、資料表、受保護路由、資料存取層回歸測試與隔離建立／更新／還原操作均已完成；SQL 已確認刪除後帳號、日記、事件與版本資料均已移除。
- [x] 將事件版本歷程、帳號級刪除介面與回歸測試鏡像同步至 GitHub `main`（最新家庭邀請接受頁同步提交 `b770b26`），並確認 Windows 副本曾通過型別檢查、53 項測試與正式建置；受管理環境最新版已通過 68 項測試與正式建置。
- [ ] 拆分 `server/db.ts` 為日記、分享、版本與帳號等專責資料存取模組，並將仍集中於 `DiaryEditor.tsx` 的專屬樣式整理為可維護模組後，再完成重構父項。
- [x] 抽離帳號資料存取至 `server/db/account.ts`，以 `server/db.ts` 的相容匯出維持 OAuth、工作階段與既有測試契約；並將事件版本快照解析抽離至 `server/db/revisions.ts` 與獨立回歸測試。
- [x] 抽離分享 slug 與私密 token 產生器至 `server/shareAccess.ts`，使公開閱讀、私密連結與家庭邀請共用經測試的安全亂數工具。
- [x] 將年度回顧與 AI 年度回顧樣式抽離至 `client/src/styles/annual-review.css`，保留原有響應式規則並降低全域樣式檔耦合。
- [x] 將日記事件、標籤與媒體彙整查詢抽離至 `server/db/diaryRead.ts`，由快照、AI 回顧與公開閱讀顯式傳入資料庫連線並維持回傳結構。
- [x] 將公開分享設定與公開故事讀取抽離至 `server/db/sharing.ts`，保留密碼／token／到期安全規則、公開事件限制與非識別性存取紀錄，並新增資料層回歸測試。
- [x] 將帳號刪除與事件版本歷程樣式抽離至 `client/src/styles/account-revisions.css`，保留既有桌面與窄螢幕配置規則。
- [x] 實作媒體檔安全打包匯出與受控匯入：以 Chronicle ZIP manifest 打包事件圖片位元組、檔名／MIME／說明與事件指紋；匯入前驗證格式、路徑、圖片類型、單檔與總量限制，僅還原至標題與發生時間相符的事件。
- [x] 在事件編輯器提供不送出日記內容、可直接套用的寫作引導：依事件類型提供本機模板，附加而不覆寫草稿，且不呼叫外部 AI 或傳送日記內容。
- [x] 建立英文 README 與中英文文件入口，清楚說明隱私、核心功能、測試與在地開發方式。
- [x] 建立本機社群草稿 JSON 解析器：支援使用者提供的噗浪相容 `plurks` 與通用 `posts` JSON，僅讀取日期、原文與來源 ID；在瀏覽器本機去重並標記重大事件候選，不使用平台帳號授權或自動發布。
- [x] 擴充社群草稿解析器支援 CSV：以日期、原文與來源 ID 欄位轉為既有本機候選流程；完整品質檢查已通過。
- [x] 在社群匯入工作台提供候選事件預覽、去重說明與人工確認建立流程；確認前不寫入日記、不連接社群帳號，確認後一律建立為私人事件。
- [x] 強化社群草稿 CSV 解析器，正確支援引號、逗號與多行欄位，並補上對應回歸測試。
- [x] 建立家庭共寫的日記成員角色、一次性邀請、事件註解與不可變稽核紀錄資料模型；擁有者管理分享與成員，editor 可在指定家庭日記共寫，commenter 僅可閱讀與註解。遷移已套用，路由回歸測試涵蓋邀請、註解與角色輸入邊界。
- [x] 在編輯器完成家庭成員清單、邀請接受入口、角色調整／移除與稽核紀錄閱讀介面，再完成家庭共寫父項。
- [x] 實際把家庭成員角色接入日記事件寫入權限：editor 可進行共寫操作，commenter 僅可讀取／註解，並補上資料層與路由回歸測試。
- [x] 修正家庭稽核紀錄 UI 對 `member_role_updated` 的顯示邏輯，避免把角色調整誤標為發表註解，並補上對應測試。
- [x] 隱藏或停用 commenter 的事件、圖片、排序、可見度與版本還原控制項，保留僅閱讀與註解的介面，並補上可測試的權限呈現規則。
- [x] 將家庭角色邊界延伸至人生階段、AI 回顧與帳號級設定控制項，確保這些設定僅由日記擁有者操作。
- [x] 將 `/editor` 載入狀態判定抽成純函式，確保查詢錯誤或逾時優先進入含重試／重新載入操作的恢復分支，並補上單元測試。
- [x] 補強快速記事 localStorage 序列化往返回歸測試，確認輸入內容與更新時間可於重新整理後還原。
- [x] 為公開故事補上成功資料抵達後的封面替代文字與 editorial／gallery／minimal 版型結構性回歸測試；真實 375px 視覺連結仍依既有待辦追蹤。
